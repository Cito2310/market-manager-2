export interface FormImage {
    nameImage: string;
    base64: string;
    uploadedAt: string;
}

export interface Image {
    nameImage: string;
    base64: string;
    uploadedAt: string;
    isActive: boolean;
    _id: string;
}